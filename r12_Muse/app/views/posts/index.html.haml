- @posts.each do |post|
  = image_tag post.image.url(:small)
  %h2= link_to post.title, post
  
  %p
    Shared By
    = post.user.name
    
  %p= pluralize(post.comments.count, "Comment")
  
= link_to "New Muse", new_post_path
